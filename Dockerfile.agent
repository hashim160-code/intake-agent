FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DEFAULT_TIMEOUT=600 \
    PIP_RETRIES=5

WORKDIR /app

COPY pyproject.toml ./ 
COPY src ./src

RUN pip install --upgrade "pip<25" \
    && pip install .

CMD ["python", "-m", "src.worker_service"]
